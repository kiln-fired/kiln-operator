---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: chainkeys.bitcoin.kiln-fired.github.io
spec:
  group: bitcoin.kiln-fired.github.io
  names:
    kind: ChainKey
    listKind: ChainKeyList
    plural: chainkeys
    singular: chainkey
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChainKey is the Schema for the chainkeys API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ChainKeySpec defines the desired state of ChainKey
            properties:
              mnemonic:
                description: BIP39 mnemonic phrase (only 24-word format supported)
                type: string
              network:
                default: simnet
                description: Bitcoin network, e.g. simnet, testnet, regressionnet,
                  mainnet
                type: string
              passphrase:
                description: User supplied passphrase for to generate HD seed
                type: string
              secretName:
                description: Name of secret to store master key, chain keypair, and
                  chain address
                type: string
            required:
            - secretName
            type: object
          status:
            description: ChainKeyStatus defines the observed state of ChainKey
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
