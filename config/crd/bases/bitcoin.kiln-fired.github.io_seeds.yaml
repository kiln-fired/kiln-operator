---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  creationTimestamp: null
  name: seeds.bitcoin.kiln-fired.github.io
spec:
  group: bitcoin.kiln-fired.github.io
  names:
    kind: Seed
    listKind: SeedList
    plural: seeds
    singular: seed
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Seed is the Schema for the seeds API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SeedSpec defines the desired state of Seed
            properties:
              import:
                description: Optional Secret reference containing aezeed mnemonic and passphrase input. When omitted, Kiln generates new seed material.
                properties:
                  mnemonicKey:
                    default: mnemonic
                    description: Name of the secret key that contains the mnemonic seed
                    type: string
                  passphraseKey:
                    default: passphrase
                    description: Name of the secret key that contains the seed passphrase
                    type: string
                  secretName:
                    description: Name of the secret that contains the seed to import
                    type: string
                type: object
              network:
                default: simnet
                description: Bitcoin network used to derive the root key.
                enum:
                - simnet
                - mainnet
                type: string
              secretName:
                description: Name of the retained Secret that stores generated/imported seed material.
                minLength: 1
                type: string
            required:
            - secretName
            type: object
            x-kubernetes-validations:
            - message: seed configuration is immutable
              rule: self == oldSelf
            - message: import Secret must differ from output secretName
              rule: '!has(self.import) || self.import.secretName != self.secretName'
          status:
            description: SeedStatus defines the observed state of Seed.
            properties:
              conditions:
                description: Conditions summarize seed generation/import and retained Secret publication.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\\/)?(([A-Z]([a-z0-9]*[A-Z0-9])*)|([a-z0-9]([a-z0-9]*[A-Z0-9])*))$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              secretName:
                description: SecretName is the retained Secret containing the resulting seed material.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
